<UserControl x:Class="Ninance_v2.MVVM.View.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ninance_v2.MVVM.View"
             xmlns:viewModel="clr-namespace:Ninance_v2.MVVM.ViewModel"
             xmlns:util="clr-namespace:Ninance_v2.Core"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:TransactionsViewModel/>
    </UserControl.DataContext>
    <Grid>
        <ListView util:GridViewSort.AutoSort="True" Background="#272537" AlternationCount="2" ItemContainerStyle="{StaticResource TransactionListItemTheme}" Style="{StaticResource TransactionListTheme}" ItemsSource="{Binding Transactions}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" Width="30" DisplayMemberBinding="{Binding Id}" util:GridViewSort.PropertyName="Id"/>
                    <GridViewColumn Header="Usage" Width="300" DisplayMemberBinding="{Binding Usage}"/>
                    <GridViewColumn Header="Date" Width="100"/>
                    <GridViewColumn Header="Amount" Width="75" DisplayMemberBinding="{Binding Amount}" util:GridViewSort.PropertyName="Amount"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
